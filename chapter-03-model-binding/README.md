# Capítulo 03: Model Binding e Contratos de Entrada

Demonstração de extração de parâmetros HTTP a partir de múltiplas fontes (`[FromHeader]`, `[FromRoute]`, `[FromQuery]`), upload de arquivos `multipart/form-data` e o método `QUERY` (RFC 10008).

## Como Executar

```powershell
powershell -ExecutionPolicy Bypass -File .\Test.Chapter03_ModelBindingApp.ps1
```
